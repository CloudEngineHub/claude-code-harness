# v5.1.0 Integration Goal Runbook — Phase 114/115 完遂オーケストレーション

作成: 2026-07-15 / 正本: `Plans.md` Phase 114/115（本書は実行層。矛盾時は Plans.md 優先）
検証: subagent 4 体（Skeptic/QA・Product/Arch・gate 棚卸し・デグレ red-team）の実測に基づく

## 1. Goal state（G1–G5、全達成で完遂）

- **G1**: origin/main が release 内容と一致し v5.1.0 公開済み — semver tag `v5.1.0` + plugin tag `claude-code-harness--v5.1.0` + GitHub Release + 4 binary assets + v5.0.0→v5.1.0 upgrade 実測 PASS
- **G2**: PR #239 が MERGED。最終 main の重複 7 ファイルは port 版内容と一致
- **G3**: デグレ 0 — AR-1〜AR-16 が各 checkpoint で全 green
- **G4**: LSP/AST トリガーが skills 5 ファイル + `docs/spec/workflow-review-and-release.md` に配線、harness-mem §NNN + XR-Registry 起票済み
- **G5**: 統治トレイル完備 — 112.20/112.21 supersession 記録済（Phase 114 preamble）、squash 裁定 codify、handoff 更新、superseded PR/issue closeout

## 2. Baseline（2026-07-15 実測値）

| 項目 | 値 |
|---|---|
| release/v5.1.0 HEAD | `98ef50ad`（origin/main `8dacf9ec` +33 / -0、割り込み commit 0 実測） |
| PR #239 | head `f6e7d79f`、CLEAN/MERGEABLE、CI 全 green、内部レビュー APPROVE |
| port branch | `feat/hermes-candidate-port-v5.1.0` = `dff6c585`（release HEAD と merge-tree conflict 0 実測） |
| validate-plugin | 合格 120 / 警告 0 / 失敗 0 |
| retired-alias scan | Entries 7 / 0 residue hits |
| plans-format-check | ok / CHANGELOG `[Unreleased]`: `### Added`×1 `### Fixed`×1 |
| release-preflight | 24 passed 0 failed（`11bc0451` 時点、`--check-adapters`） |
| version 表面 | 6 文字列 / 5 ファイル全て `5.0.0`（VERSION, .claude-plugin/plugin.json, .codex-plugin/plugin.json, .cursor-plugin/plugin.json, .claude-plugin/marketplace.json×2, harness.toml） |

## 3. Anti-Regression matrix（AR-1〜AR-16）

判定は全て機械コマンド。「等値」でなく「床 + 失敗 0」で書く（テスト追加を偽陽性にしない）。

| AR | 判定コマンド | 合格条件 | checkpoint |
|---|---|---|---|
| AR-1 | `bash tests/validate-plugin.sh` | 失敗=0 かつ 合格>=120 | 114.1/114.3/114.4/114.6/115.2 後 |
| AR-2 | `cd go && go test ./... -count=1` | 0 fail | 114.4 / 114.6 |
| AR-3 | `bash scripts/ci/check-consistency.sh` | exit 0（binary drift 含む） | 各 merge 後 |
| AR-4 | `bash scripts/release-preflight.sh --check-adapters` | 0 failed。**`--check-adapters` 必須**（無しだと約 12 check が無言 skip） | 114.4 / 114.6 |
| AR-5 | `git diff dff6c585 HEAD -- <7 files>` | 差分が意図分のみ（SA-2 が per-file 判定） | 114.3 後 |
| AR-6 | `awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md \| grep -c '^### Added'` | `### Added`=1 かつ `### Fixed`=1 | 114.3 後 |
| AR-7 | `bash tests/test-support-claim-wording.sh && bash tests/test-support-claim-wording-selftest.sh` | 両方 exit 0 | 114.3 / 115.2 後 |
| AR-8 | `./scripts/sync-skill-mirrors.sh --check && bin/harness mirror verify --json` | healthy:true | skills 変更後 |
| AR-9 | scratchpad へ `git clone` → `bash tests/validate-plugin.sh` | 失敗 0（gitignore trap 検知） | 114.4（GO 前） |
| AR-10 | `bin/harness retired-alias scan` | 0 hits | 114.4 / 115.2 |
| AR-11 | 6 version 文字列の grep | GO まで全て 5.0.0 / bump 後全て 5.1.0。**変更は `./scripts/sync-version.sh bump` のみ**（手動 sed/Edit 禁止） | 114.5 まで / 114.6 |
| AR-12 | `bash scripts/plans-format-check.sh` | ok | Plans.md 変更後 |
| AR-13 | `! grep -qi hermes hosts/registry.json && bash tests/test-hermes-agent-candidate.sh` | 両方成立（test は 114.1 の port merge で着地） | 114.1 後 |
| AR-14 | `bash scripts/ci/check-i18n-hookhandler-ratchet.sh` | exit 0 | 114.4 |
| AR-15 | `bash tests/test-guardrails-r01-r13.sh` | exit 0（R01-R13 policy engine の名指し再検証） | 114.4 |
| AR-16 | `bash scripts/ci/check-branch-alignment-ledger.sh` | exit 0（squash 禁止裁定の機械化 — Plans.md hash 台帳 vs ancestry） | 114.2 前後 / 114.6 merge 前 |

補助: `bash tests/test-claude-upstream-integration.sh`（115.2 で SKILL.md を触るため 114.4/115.2 で実行）、`bin/harness doctor`（114.4 で 1 回）。

## 4. Rollback matrix（デグレ保険）

| 段 | 事前保険 | 戻し方 |
|---|---|---|
| 114.1 / 114.3 / 114.5 | merge 直前に `git branch backup/pre-<task番号> HEAD` | push 前なので可逆。`git switch -C release/v5.1.0 backup/pre-<task番号>`（`git reset --hard` は deny 対象のため使わない。切替は operator 承認の上で実行） |
| 114.2（#239 merge） | AR-16 + CI green + APPROVE 確認後にのみ押す | 不可逆（外部送信）。問題時は GitHub 上の Revert PR で前進的に戻す |
| 114.6（tag/Release） | HG-2 単一 GO + `claude plugin tag .claude-plugin --dry-run` 事前検証 | tag 削除は禁止（versioning.md）。問題時は v5.1.1 hotfix で前進。plugin 環境の rollback は `~/Library/Application Support/CCH-backups/20260713-pre-5.0.0-align` |
| 115.x | codex 実装は isolated worktree（fingerprint 比較 + Lead diff review + cherry-pick） | cherry-pick 前なら worktree 破棄のみ |

## 5. Human gates

- **HG-1**: 事前確認 4 件の承認 → `.claude/state/plan-preapprovals.json` に `plan-preapproval.v1` で記録（114.2 PR merge / 114.6 push・PR・tag・Release・closeout / 115 PR / 115.4 cross-repo write）
- **HG-2**: v5.1.0 単一リリース GO（114.6 直前。従来契約どおり、事前確認では代替しない）
- **HG-3**（任意・推奨）: `.github/workflows/validate-plugin.yml` へ `bash ./tests/test-hermes-agent-candidate.sh` の 1 行追加。workflows は AI 編集 deny のため **operator 手動 patch**（red-team 指摘 #1: 現状この test は CI 未配線）

## 6. Stage plan + Subagent fleet

実行モデルの前提:
- Claude 系 subagent は env `CLAUDE_CODE_SUBAGENT_MODEL=claude-sonnet-5` で **全て Sonnet 5 固定**（per-call model 指定は効かない）。深さは prompt 指示とスコープで制御
- codex 委譲は `scripts/codex-companion.sh` 経由のみ。review tier は gpt-5.5/xhigh（正本 `scripts/model-routing.sh`）
- 敵対 review の primary verdict（APPROVE|REQUEST_CHANGES）は Lead（brain）が出す。critical 検出時の修正ループは最大 3 回

### Stage 114.1 — port merge（executor: Lead @ release worktree）

手順: `git branch backup/pre-114.1 HEAD` → `git merge --no-ff origin/feat/hermes-candidate-port-v5.1.0` → AR-13/AR-1/AR-12。

**SA-1 merge-verifier-1141**（general-purpose / sonnet-5 / effort: medium / read-only）

```text
あなたは merge 検証官。対象 /private/tmp/cch-release-v5.1.0（読み取りとテスト実行のみ、書込・git 変更禁止）。
直前に port branch dff6c585 が merge された。次を実測し verdict を返せ:
(1) bash tests/test-tool-first-onboarding.sh → PASS（既知 RED が解消されたこと）
(2) bash tests/test-hermes-agent-candidate.sh → PASS
(3) ! grep -qi hermes hosts/registry.json → 成立
(4) git diff --name-only backup/pre-114.1..HEAD の全ファイルが git diff --name-only $(git merge-base backup/pre-114.1 dff6c585)..dff6c585 の集合に含まれる（想定外ファイル混入 0）
(5) bash tests/validate-plugin.sh → 失敗 0 かつ 合格>=120
出力: 各項目に PASS/FAIL + コマンド出力の要点。1 件でも FAIL なら verdict=BROKEN と rollback 推奨（backup/pre-114.1）を明記。
```

### Stage 114.2 — PR #239 merge（executor: Lead、外部送信 = HG-1）

手順: `bash scripts/ci/check-branch-alignment-ledger.sh`（AR-16）→ `gh pr view 239 --json mergeable,statusCheckRollup` で CLEAN + 全 SUCCESS 再確認 → `gh pr merge 239 --repo Chachamaru127/claude-code-harness --merge`（**merge commit。squash/rebase 禁止**）→ main の CI green を `gh run list --branch main --limit 3` で確認。local main は push しない（binary 未リビルドのため drift gate red になる。Phase 111 は release PR 経由で main へ）。

### Stage 114.3 — main→release sync + conflict 解消（executor: Lead）

手順: `git branch backup/pre-114.3 HEAD` → `git fetch origin && git merge origin/main` → 既知 7 ファイル（Plans.md / README.md / README_ja.md / docs/onboarding/index.md / docs/tool-capability-matrix.md / tests/test-support-claim-wording.sh / tests/test-tool-capability-matrix.sh）を **release/port 側優先**で解消 → Plans.md の Phase 113 行を台帳順に統合 → CHANGELOG `[Unreleased]` を単一見出し構造へ手動復元（auto-merge が無音で二重化する実測あり）→ AR-6/AR-12。

**SA-2 conflict-auditor-1143**（general-purpose / sonnet-5 / effort: high / read-only）

```text
あなたは conflict 解消の監査官。対象 /private/tmp/cch-release-v5.1.0（読み取りとテスト実行のみ）。
直前に origin/main が merge され、7 ファイルの conflict が「release/port 側優先」で解消された。深く検証せよ:
(1) 7 ファイル各々: git diff dff6c585 HEAD -- <file> を読み、port 版から乖離した行が「#239 由来の正当な追加（Hermes candidate 行等）」以外に無いこと。1 ファイルずつ CONFIRMED / BROKEN(該当行引用) で判定
(2) docs/tool-capability-matrix.md と README/README_ja に Hermes 行（candidate tier）と Grok/Cursor 行（internal-compatible tier）が共存し矛盾表記が無いこと
(3) CHANGELOG: awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md に対し grep -c '^### Added' と '^### Fixed' が各 1、'^## \[Unreleased\]' の総出現が 1
(4) bash scripts/plans-format-check.sh → ok。Plans.md に Phase 113 が存在し cc:done [fa2b9c37] 等の hash が保持されていること
(5) bash tests/test-support-claim-wording.sh && bash tests/test-support-claim-wording-selftest.sh && bash tests/test-tool-capability-matrix.sh → 全 PASS
(6) git rev-list --count HEAD..origin/main → 0
出力: 項目別 verdict + evidence。BROKEN が 1 件でもあれば全体 verdict=REQUEST_CHANGES。
```

**SA-3 fresh-clone-gate**（general-purpose / sonnet-5 / effort: low / SA-2 と並列）

```text
scratchpad 配下に /private/tmp/cch-release-v5.1.0 を git clone し（clone 先は新規 temp dir）、
clone 側で (1) bash tests/validate-plugin.sh（失敗 0 / 合格>=120）、(2) bash tests/test-hermes-agent-candidate.sh、
(3) ls docs/research/hermes-agent-candidate.md docs/research/grok-adapter-candidate.md（gitignore trap で欠落していないこと）を実測。
元 worktree への書込禁止。出力: 3 項目の PASS/FAIL + 欠落ファイルがあれば .gitignore の該当行番号。終了後 clone dir を削除。
```

### Stage 114.4 — 全 gate 再実行 + delta review（executor: Lead + fleet 4 並列）

Lead が順に実行: AR-2 → AR-1 → AR-3 → AR-4（`--check-adapters`）→ AR-10 → AR-14 → AR-15 → `bash tests/test-claude-upstream-integration.sh` → `bin/harness doctor` → AR-9（SA-3 を GO 前に再走）。全 green 後に review fleet を**並列 4 体**起動:

**SA-4 reviewer-security**（claude-code-harness:reviewer / sonnet-5 / effort: high / read-only）

```text
fresh-context の security reviewer として、/private/tmp/cch-release-v5.1.0 の git diff 98ef50ad..HEAD を全件レビューせよ。
レンズ: (a) .github/workflows/ 差分の CI 挙動変化（追加のみか、既存 step の削除・弱体化が無いか）、(b) hooks/permissions/deny 面の変化、
(c) secret・credential・トークンの混入、(d) hosts/registry.json と support tier 表記の過大主張。
判定基準: critical（公開物の安全性・権限を毀損）/ major（gate の弱体化）/ minor。
出力: review-result.v1 形式（verdict: APPROVE | REQUEST_CHANGES、critical_issues[] / major_issues[] / recommendations[]、各 issue に file:line）。
```

**SA-5 reviewer-regression**（claude-code-harness:reviewer / sonnet-5 / effort: high / read-only）

```text
fresh-context の regression reviewer として、/private/tmp/cch-release-v5.1.0 の git diff 98ef50ad..HEAD をレビューせよ。
レンズ: (a) 既存テストの削除・弱体化（アサーション減、期待値の緩和）が無いか、(b) docs/plans/v5.1.0-integration-runbook.md の AR-1〜AR-16 に対応する gate が全て現存するか、
(c) tests/ の新規ファイルが .github/workflows/validate-plugin.yml に配線されているか（未配線は major でなく minor + 明記）、(d) Plans.md の cc: マーカー改変が意図分のみか。
出力: review-result.v1 形式（verdict / critical_issues / major_issues / recommendations、file:line 付き）。
```

**SA-6 reviewer-spec-claims**（claude-code-harness:reviewer / sonnet-5 / effort: high / read-only）

```text
fresh-context の spec/claims reviewer として、/private/tmp/cch-release-v5.1.0 の git diff 98ef50ad..HEAD をレビューせよ。
レンズ: (a) spec.md / docs/spec/*.md と実装・docs の言行一致、(b) README / README_ja / docs/onboarding / docs/tool-capability-matrix の host tier 主張が
test pin と一致（Hermes=candidate、Grok/Cursor=internal-compatible、supported の過大主張ゼロ）、(c) CHANGELOG [Unreleased] の記述が実差分と一致、
(d) Phase 114 preamble の supersession 記録（112.12/112.20/112.21）が残っていること。
出力: review-result.v1 形式（verdict / critical_issues / major_issues / recommendations、file:line 付き）。
```

**SA-7 codex-adversarial**（Lead が Bash で実行 / gpt-5.5 xhigh / advisory）

```bash
cd /private/tmp/cch-release-v5.1.0 && bash scripts/codex-companion.sh review --base 98ef50ad
```

**verdict 合成（Lead = brain）**: SA-4/5/6 の 3 verdict + SA-7 advisory を統合。critical 0 かつ major は全て解消 or 明示 defer 記録で APPROVE。critical 検出 → 修正 → 再 review、最大 3 ループ。3 回超は operator へ decision_needed。

### Stage 114.5 — 記録更新 + 恒久化（executor: Lead）

1. `handoff-artifact.json` / `cch-v5.1.0-release-handoff.md` を新 HEAD で更新（status は `awaiting_operator_release_confirmation` 維持、PR #239 boundary 行を「merged & included」へ）
2. `.claude/rules/github-release.md` にマージ方式節を追記（release PR = merge commit、Plans.md hash 台帳がある限り squash 不採用、AR-16 が機械ゲート）
3. `scripts/ci/check-consistency.sh` に `[Unreleased]` 単一性チェック（`## [Unreleased]` 出現 1 回 + セクション内 `### Added`/`### Fixed` 各<=1）を追加（red-team #2 の恒久化）
4. AR-12 + AR-3 で自己検証

### Stage 114.6 — GO 後リリース（executor: Lead、HG-2 必須）

**version bump は `./scripts/sync-version.sh bump` のみ**（6 文字列/5 ファイル同期。手動 sed/Edit 禁止 — red-team #4 major）。

手順: HG-2 GO → sync-version.sh bump 5.1.0 → CHANGELOG promote → binary 4 platform 再生成（`scripts/build-all.sh`、toolchain pin 順守）→ AR-1〜4/10/11/16 再走 → release branch push → release PR 作成 → CI 完走 → **merge commit** で main へ → **tag 二重体系**: `claude plugin tag .claude-plugin --dry-run` で検証後 plugin tag `claude-code-harness--v5.1.0` + semver `v5.1.0` を merged main から作成・push（red-team #5 major: 片方だけで完了と誤認しない）→ GitHub Release + 4 assets → trunk（main checkout）で `./scripts/sync-skill-mirrors.sh && bash scripts/ci/check-consistency.sh`（isolated-green trunk-red の 5 連鎖対策）。

**SA-8 release-smoke-verifier**（general-purpose / sonnet-5 / effort: medium）

```text
v5.1.0 公開後の smoke 検証官。次を実測し evidence 付きで報告:
(1) gh release view v5.1.0 --json assets → 4 binary assets（darwin-arm64/darwin-amd64/linux-amd64/windows-amd64）が存在
(2) git tag の 2 本（claude-code-harness--v5.1.0 と v5.1.0）が merged main の commit を指す
(3) .claude-plugin/marketplace.json の 2 箇所と VERSION/plugin.json×3/harness.toml が全て 5.1.0
(4) Claude Code と Codex で v5.0.0→v5.1.0 の update 手順を実行し、bin/harness version が 5.1.0 を返す
出力: 112.12 DoD (f)(g)(h) に対応する evidence 一覧。FAIL があれば該当 DoD 記号を明示。
```

**SA-9 closeout-scribe**（general-purpose / sonnet-5 / effort: low）

```text
published evidence に基づく closeout 文の起草官（投稿はしない。draft のみ）。
#228 / #237 / #231 / #238 それぞれについて、v5.1.0 の該当 commit/release URL を引いた close コメント英文 draft を作成。
PR #239 には v5.1.0 に含まれた旨の追記 draft。各 draft は 5 行以内、リンクは実在確認済みのものだけ使う。
出力: issue/PR 番号ごとの draft 一覧（Lead が HG-1 承認範囲内で投稿する）。
```

### Stage 115 — LSP/AST 配線（リリース後、main checkout。実装は codex 委譲）

**SA-10 codex-impl**（Lead が Bash で実行 / gpt-5.5 / isolated worktree + Lead diff review + cherry-pick）

```bash
bash scripts/codex-companion.sh task --write "$(cat <<'PROMPT'
TDD で 2 commit に分けて実装せよ。対象 repo はこの worktree。
[commit 1 = RED] tests/test-lsp-workflow-wiring.sh を新設。tests/test-claude-upstream-integration.sh の形式（set -euo pipefail / 対象ファイル配列 / grep -q literal / 失敗時 echo+exit 1）を踏襲し、次の 5 ファイルに後述 literal が存在することを pin: skills/harness-work/SKILL.md, skills-codex/harness-work/SKILL.md, skills/harness-review/SKILL.md, skills/breezing/SKILL.md, skills-codex/breezing/SKILL.md。この時点で test は RED（bash 実行ログを commit message に残す）。
[commit 2 = GREEN] 5 ファイルへ追記: (i) 「同一シンボルを同一セッションで 2 回 grep したら harness_ast_search へ切り替える」、(ii) 「同型実装が複数 module に並ぶ bugfix は修正前に harness_ast_search で全実装検索を必須とする」、(iii) 「変更ファイルに .ts/.tsx を含む場合のみ harness_lsp_diagnostics の新規エラー 0 を DoD とする。harness MCP 未接続または非対象ファイル型は not-configured として満了扱いにし blocking しない」。配置: harness-work は Sprint Contract の runtime_validation 節、harness-review は合格ライン（regression safety）節、breezing は DoD 言及節。harness_lsp_references は使わない（instruction stub のため）。docs/spec/workflow-review-and-release.md に同契約を 2 行追記。
完了条件: bash tests/test-lsp-workflow-wiring.sh PASS、bash tests/validate-plugin.sh 失敗 0、bash tests/test-claude-upstream-integration.sh PASS。
PROMPT
)"
```

Lead 後処理: diff review → cherry-pick → `./scripts/sync-skill-mirrors.sh` → AR-8/AR-1/AR-7 → PR 作成（HG-1 範囲）。

**SA-11 codex-review-primary**（Lead が Bash で実行 / gpt-5.5 xhigh / primary review per review.mode=codex）

```bash
bash scripts/codex-companion.sh review --base <cherry-pick 前の main HEAD>
```

verdict mapping: approve→APPROVE / needs-attention→REQUEST_CHANGES（codex-cli-only.md の表に従う）。second opinion が要る場合のみ `claude -p --model opus` で Opus に同 diff を読ませる。

**SA-12 xr-scribe**（general-purpose / sonnet-5 / effort: medium / 115.4、draft のみ）

```text
cross-repo 起票の起草官（sibling repo への書込はしない。draft のみ）。
harness-mem 向け Plans.md §NNN エントリを起草: タイトル「harness_lsp_* の実態合わせ — references/definition/hover は instruction stub、diagnostics は .ts/.tsx のみ」。
本文に含める: (a) evidence: mcp-server-go/internal/tools/codeintel.go:170-247（handleLspRefs/Def/Hover が fmt.Sprintf の固定文言、diagnostics は strings.HasSuffix .ts/.tsx のみ）、
(b) DoD 案: Go/shell diagnostics 対応 or tool description の実態合わせのどちらかを選択して実装、(c) claude-code-harness 側は skills 配線のみ実施済みで実装しない boundary、
(d) XR-Registry.md 追加行の draft（owner: harness-mem / impacted: claude-code-harness / trigger: 2026-07-15 LSP 0-usage 調査）。
出力: §NNN 本文 draft + XR 行 draft（Lead が HG-3 系承認後に書き込む）。
```

**SA-13 completeness-critic**（general-purpose / sonnet-5 / effort: medium / Phase 114 完了時と 115 完了時に各 1 回）

```text
完遂監査官。docs/plans/v5.1.0-integration-runbook.md の G1〜G5 を読み、現在の repo/GitHub 状態と突き合わせて
「evidence が存在しない goal 項目」「実行されなかった AR」「宣言と実態の乖離」を列挙せよ（読み取り + gh read-only のみ）。
出力: goal 項目ごとに DONE(evidence) / MISSING(何が無いか)。MISSING は follow-up task 案 1 行付き。
```

## 7. Fleet 一覧（要約）

| ID | 役割 | 実体 / model / effort | 段 |
|---|---|---|---|
| SA-1 | merge 検証 | general-purpose / Sonnet 5 / med | 114.1 後 |
| SA-2 | conflict 監査 | general-purpose / Sonnet 5 / high | 114.3 後 |
| SA-3 | fresh-clone gate | general-purpose / Sonnet 5 / low | 114.3 後・GO 前 |
| SA-4/5/6 | review 3 視点 | claude-code-harness:reviewer / Sonnet 5 / high | 114.4（並列） |
| SA-7 | 敵対 review | codex-companion review / gpt-5.5 xhigh | 114.4（並列） |
| SA-8 | release smoke | general-purpose / Sonnet 5 / med | 114.6 後 |
| SA-9 | closeout 起草 | general-purpose / Sonnet 5 / low | 114.6 後 |
| SA-10 | 実装（TDD 2 commit） | codex-companion task --write / gpt-5.5 | 115.1-115.3 |
| SA-11 | primary review | codex-companion review / gpt-5.5 xhigh | 115 PR 前 |
| SA-12 | XR 起票起草 | general-purpose / Sonnet 5 / med | 115.4 |
| SA-13 | 完遂監査 | general-purpose / Sonnet 5 / med | 各 Phase 末 |
